def unsafe?(speed)
  return true if speed > 60
  speed > 60 ? true : false
  speed < 40 ? true : false
  

end



def not_safe?(speed)
	 return true if speed > 60
	 speed > 60 ? true : false
	 speed < 40 ? true : false
	
	 
end
	


